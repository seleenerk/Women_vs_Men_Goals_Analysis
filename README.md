# Women vs Men Goals Analysis

This project analyzes FIFA World Cup matches since 2002 to determine if women's matches have more goals than men's matches.

## Features
- Data filtering for FIFA World Cup matches after 2002
- Histogram and boxplot visualizations
- Mann–Whitney U test (one-tailed) to compare goals
- Clear interpretation of results

## Data
CSV files for men's and women's match results are included in the `data/` folder.

## Usage
Open the Jupyter Notebook `Women_vs_Men_Goals_Analysis.ipynb` and run all cells.
